[project]
name = "slack-mcp"
version = "0.1.0"
description = "MCP server for Slack integration"
requires-python = ">=3.10"
dependencies = [
    "slack-sdk>=3.27.0",
    "mcp>=1.0.0",
]

[project.scripts]
slack-mcp = "server:main"

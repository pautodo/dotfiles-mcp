[project]
name = "athena-mcp"
version = "0.1.0"
description = "MCP server for AWS Athena queries"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34.0",
    "awswrangler>=3.5.0",
    "pandas>=2.0.0",
    "mcp>=1.0.0",
    "tabulate>=0.9.0",
]

[project.scripts]
athena-mcp = "server:main"
